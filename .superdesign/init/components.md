# Shared Components

## ObservationsPanel
- Source: `frontend/src/components/ObservationsPanel.jsx`
- Description: Shared observations panel used by card-management pages.

```jsx
// Source file is intentionally referenced for the full implementation.
// The frontend has no shared UI primitive library; most controls are local helpers in page files.
```

## Local primitives
`ManagementDashboard.jsx` defines the reusable page-local primitives `Title`, `Panel`, `Card`, `Input`, `Detail`, `Empty`, and `Notice`. Their implementations are included in the target page context; there is no separate design-system component directory.
